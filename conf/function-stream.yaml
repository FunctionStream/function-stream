listen_addr: ":7300"
tube_factory:
  pulsar:
    type: "pulsar"
    config:
      pulsar_url: "pulsar://localhost:6650"
  memory:
    Type: "memory"
  default:
    ref: "pulsar"
runtime_factory:
  wasm:
    type: "wasm"
  default:
    ref: "wasm"
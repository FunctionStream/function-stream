name: Eslint Check
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: install dependencies
        run: |
          npm install eslint@5.16.0
          npm install @vue/eslint-config-standard@4.0.0
          npm install eslint-plugin-vue@5.2.3
          npm install babel-eslint@10.0.1
          npm install eslint-plugin-html@5.0.0
        working-directory: front-end

      - name: run eslint
        run: npx eslint . --ext .js,.jsx,.ts,.vue,.tsx
        working-directory: front-end

/* import Mock from 'mockjs'

// 判断环境不是 prod 或者 preview 是 true 时，加载 mock 服务
if (import.meta.env.DEV) {
  // 使用同步加载依赖
  console.log('[dev]  mock mounting...')
  import('./services/func')

  Mock.setup({
    timeout: 800 // setter delay time
  })
  console.log('[dev] mock mounted')
}
 */
